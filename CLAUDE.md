# claudecatraz

Read [`docs/RULES.md`](docs/RULES.md) before writing code, docstrings, or
commits — it is the binding set of repo conventions (docstring style, commit
identity, fail-closed posture, action-model invariants).

Architecture background lives in `docs/design/`; the current target model for
warden actions is `docs/design/architecture-generalization/10-actions-rework.md`.
