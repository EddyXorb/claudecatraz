"""GitLab policy-enforcement proxy (Warden) for the agent sandbox.

API write-filter, git Smart-HTTP proxy, durable quota state, and auditable
JSONL logging — the single trust boundary between the agent and gitlab.com.
"""

__version__ = "0.1.0"
