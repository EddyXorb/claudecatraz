"""GitLab forge domain: credentials, upstream, service-account, MR-ownership, reconcile.

Shared by git and REST-API guards. Distinct from ``guards.gitlab_api`` (the REST guard itself).
"""

from __future__ import annotations
